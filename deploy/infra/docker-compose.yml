services:
  freesurfer:
    build:
      context: ../../
      dockerfile: deploy/freesurfer/Dockerfile
    container_name: freesurfer
    user: root
    volumes:
      - ${OUT_PATH}:/data
    command: /bin/bash

  ants:
    image: antsx/ants:latest
    container_name: ants
    user: root
    volumes:
      - ${OUT_PATH}:/data
    command: /bin/bash

  neurocbir:
    build:
      context: ../../
      dockerfile: deploy/docker/Dockerfile
    container_name: neurocbir
    user: root
    volumes:
      - ${OUT_PATH}:/data
      - ../configs:/configs
    entrypoint: ["/usr/local/bin/entrypoint.sh"]
